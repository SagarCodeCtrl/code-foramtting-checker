name: 'Code Formatting Checker'
description: 'Checks if code formatting guidelines are followed in pull requests.'
author: 'Sagar'
inputs:
  github-token:
    description: 'GitHub token to authenticate with the API.'
    required: true
runs:
  using: 'node20'
  main: 'index.js'
